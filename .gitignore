node_modules/

# Gradle / Android build output. The shipped APK is committed under dist/
# instead, because a build directory is not a deliverable.
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.iml
*.keystore
*.jks
local.properties

# Raspberry Pi vision bridge — the service-account key is a real credential.
# It grants admin access to the Firebase project. Never commit it.
pi/serviceAccountKey.json
pi/*.serviceaccount.json
pi/.venv/
pi/__pycache__/

.DS_Store
Thumbs.db
